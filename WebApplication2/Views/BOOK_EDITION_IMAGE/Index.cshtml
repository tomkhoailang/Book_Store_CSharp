@model IEnumerable<WebApplication2.Models.BOOK_EDITION_IMAGE>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EditionImage)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BOOK_EDITION.EditionDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EditionImage)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BOOK_EDITION.EditionDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EditionImageID }) |
            @Html.ActionLink("Details", "Details", new { id=item.EditionImageID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EditionImageID })
        </td>
    </tr>
}

</table>
