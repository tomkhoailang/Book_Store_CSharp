@model IEnumerable<WebApplication2.Models.BOOK_COLLECTION>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BookCollectionName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BookCollectionName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BookCollectionID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BookCollectionID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BookCollectionID })
        </td>
    </tr>
}

</table>
